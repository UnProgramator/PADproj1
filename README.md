# PADproj1
X and 0 game implemented in bsd socket, in c, on lynux

Install: compile both files (server.c, client.c), run them in separate terminals. eventualy for this project the code is the best documentation
